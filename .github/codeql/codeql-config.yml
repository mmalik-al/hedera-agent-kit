name: Hedera Agent Kit CodeQL Configuration

queries:
  - uses: security-extended
  - uses: security-and-quality

paths:
  - typescript

paths-ignore:
  - '**/node_modules/**'
  - 'typescript/dist/**'
  - '**/*.d.ts'
